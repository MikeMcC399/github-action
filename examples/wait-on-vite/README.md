# example: wait-on-vite

This example confirms our action can wait for [Vite.js](https://vitejs.dev/) to respond.

The `vite.config-https.js` file is an alternate configuration that serves the app over https (via `npm run dev:https`), using a self-signed certificate generated by `@vitejs/plugin-basic-ssl`.
