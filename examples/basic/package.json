{
  "name": "example-basic",
  "version": "1.0.0",
  "description": "basic example how to run Cypress tests",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "postinstall": "patch-package"
  },
  "private": true,
  "devDependencies": {
    "cypress": "14.3.0",
    "patch-package": "^8.0.0"
  }
}
